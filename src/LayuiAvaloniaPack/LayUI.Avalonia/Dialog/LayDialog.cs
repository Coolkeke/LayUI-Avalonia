using System;
using System.Collections.Generic;
using System.Text;

namespace LayUI.Avalonia
{
    public class LayDialog : ILayDialog
    {
        public void Show(string dialogName, ILayDialogParameter parameters, string tooken)
        {
            
        }

        public void Show(string dialogName, ILayDialogParameter parameters, Action<ILayDialogResult> callback, string tooken)
        {
            
        }

        public void ShowDialog(string dialogName, ILayDialogParameter parameters, string tooken)
        {
            
        }

        public void ShowDialog(string dialogName, ILayDialogParameter parameters, Action<ILayDialogResult> callback, string tooken)
        {
           
        }
    }
}
